using UnityEngine;
using System.Collections;

public class Ballet : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// iTween.Init(gameObject);
		// iTweenEvent.GetEvent(gameObject, "shot").Play();
		// tmpEvent.Play();
		// tmpEvent
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
